import React from 'react';
import { render, screen } from '@testing-library/react';
import App from './App';

test('reset btn', () => {
  // render(<App />);
  // const linkElement = screen.getByText(/reset record/i);
  // expect(linkElement).toBeInTheDocument();
});
